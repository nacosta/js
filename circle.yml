machine:
    services:
        - docker

dependencies:
    override:
        - sudo pip install 'docker-compose<=1.4'

test:
    override:
        - docker-compose run -d --no-deps node1
        - cd node; mocha
