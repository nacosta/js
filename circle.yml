machine:
    services:
        - docker

dependencies:
    override:
        - sudo pip install -U docker-compose<1.5

test:
    override:
        - docker-compose run -d --no-deps node1
        - cd node; mocha
